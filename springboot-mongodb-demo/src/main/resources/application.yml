spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      #authentication-database: admin
      #username: admin
      #password: password
      database: demo
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  servlet:
    multipart:
      max-file-size: 256MB
      max-request-size: 256MB
      enabled: true